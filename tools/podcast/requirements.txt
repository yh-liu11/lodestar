pyyaml>=6.0
# [socks] pulls in PySocks so PODCAST_PROXY=socks5://... works with requests.
requests[socks]>=2.31
yt-dlp>=2025.1.0
youtube-transcript-api>=0.6.2
# Optional: faster-whisper enables auto MP3 transcription for podcast RSS feeds
# whose <description> is short. fetch_podcasts.py degrades gracefully if it is
# missing and falls back to the RSS description text. ffmpeg must also be on PATH.
# Install separately with: pip install -r requirements-transcribe.txt
