# Examples

这里是**示例产物**，不是你的工作区内容。

`smoke-test/` 保存了 2026-06-05 维护者跑通首次摄入（`inbox/first-note.md → wiki`）和 PDF 摄入（`inbox/sample-ai-workspace.pdf → wiki`）时生成的完整产物链，供你对照：装好之后跑同样的试跑（见 `SMOKE-TEST.md`），agent 生成的文件结构应该和这里类似。

| 目录 | 对应真实位置 | 内容 |
|------|--------------|------|
| `smoke-test/wiki/` | `wiki/` | raw / sources / concepts / explorations 四层摄入产物 |
| `smoke-test/output/` | `output/` | 摄入报告和 wiki 优先读取验证 |

你的 `wiki/` 和 `output/` 从空白开始，这些示例不会被安装器复制、也不参与任何流程。看完可以整个删掉 `examples/`，不影响系统运行。

<!-- 文件说明：示例产物说明，与用户工作区内容区分。 -->
