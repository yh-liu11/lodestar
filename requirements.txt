# Lodestar - 合并依赖
# 安装：pip install -r requirements.txt
# 可复现安装用 requirements.lock（版本锁定）。

# 核心（daily-watch + screen + research 数据拉取）
# [socks] 附带 PySocks，供 podcast 工具的 PODCAST_PROXY=socks5://... 使用
requests[socks]>=2.28.0
python-dotenv>=1.0.0
pyyaml>=6.0

# 播客摄入（podcast）
yt-dlp>=2025.1.0
youtube-transcript-api>=0.6.2
# 可选：faster-whisper>=1.0.0  （音频转录，需要 ffmpeg）

# PDF 摄入见 requirements-pdf.txt（可选）
# 文件说明：基础 Python 依赖和可选能力依赖入口。
